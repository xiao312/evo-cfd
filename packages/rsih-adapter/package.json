{
  "name": "@evocfd/rsih-adapter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Narrow integration with RSI-Harness: manifests, worker env, patch validation",
  "scripts": {
    "test": "node --experimental-strip-types --test \"test/*.test.ts\""
  }
}
