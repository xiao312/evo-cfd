FoamFile
{
    version 2.0;
    format ascii;
    class volScalarField;
    location "0";
    object k;
}
dimensions [0 2 -2 0 0 0 0];
internalField uniform 0.05;
boundaryField
{
    geom { type kqRWallFunction; value uniform 0.05; }
    slipwalls { type zeroGradient; }
    noslipwalls { type kqRWallFunction; value uniform 0.05; }
    ch4inlet
    {
        type turbulentIntensityKineticEnergyInlet;
        intensity 0.05;
        value uniform 15.1209375;
    }
    loxinlet
    {
        type turbulentIntensityKineticEnergyInlet;
        intensity 0.05;
        value uniform 0.0513375;
    }
    axisymmetric { type symmetry; }
    outlet { type zeroGradient; }
    planePlus { type wedge; }
    planeMinus { type wedge; }
}

